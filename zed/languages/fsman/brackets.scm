; fsman uses braces for nested filesystem selections. The permissive fallback
; grammar exposes the document as content, so bracket behavior is configured
; in config.toml until fsman has a dedicated Tree-sitter grammar.
